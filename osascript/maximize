#!/usr/bin/osascript

set maxApplications to {"Work - Slack","librewolf", "Music","Terminal","Kitty"}

repeat with appName in maxApplications
  try
    tell application appName
        tell the front window to set zoomed to true
    end tell
  end try
end repeat
