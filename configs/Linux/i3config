# This file has been auto-generated by an i3 wrapper
# It will be overwritten anytime i3 is restarted.
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $homedir "/home/kwstannard/"

hide_edge_borders both

focus_follows_mouse no
# max terminal screen space
for_window [title=".*Konsole$"] border none
for_window [title="Terminal"] border none
for_window [class="Firefox"] border none
for_window [class="Slack"] border none

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango:DejaVu Sans Mono 12
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#set $current_workspace "
  # $(i3-msg -t get_workspaces |
    #jq -r '.[] | "(.name) (.focused)"' |
    #grep true |
    #sed 's/ w*$//')
#"
# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
  bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt

# same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

# back to normal: Enter or Escape
    bindsym Shift+Escape mode "default"
    bindsym Return exec --no-startup-id special_mode_exit
    bindsym Escape exec --no-startup-id special_mode_exit
}

set $mode_movement Movement
mode "$mode_movement" {
# move focused window
  bindsym $mod+h move left
  bindsym $mod+j move down
  bindsym $mod+k move up
  bindsym $mod+l move right

# alternatively, you can use the cursor keys:
  bindsym $mod+Left move left
  bindsym $mod+Down move down
  bindsym $mod+Up move up
  bindsym $mod+Right move right

    # back to normal: Enter or Escape
    bindsym Shift+Escape mode "default"
    bindsym Return exec --no-startup-id special_mode_exit
    bindsym Escape exec --no-startup-id special_mode_exit
}

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
  bindsym l exec --no-startup-id custom_locker, exec --no-startup-id special_mode_exit
    bindsym e exec --no-startup-id i3msg exit, exec --no-startup-id special_mode_exit
    bindsym s exec --no-startup-id custom_locker && systemctl suspend, exec --no-startup-id special_mode_exit
    bindsym h exec --no-startup-id custom_locker && systemctl hibernate, exec --no-startup-id special_mode_exit
    bindsym r exec --no-startup-id systemctl reboot, exec --no-startup-id special_mode_exit
    bindsym Shift+s exec --no-startup-id systemctl poweroff, exec --no-startup-id special_mode_exit

    # back to normal: Enter or Escape
    bindsym Shift+Escape mode "default"
    bindsym Return exec --no-startup-id special_mode_exit
    bindsym Escape exec --no-startup-id special_mode_exit
}
set $mode_environment environment
mode "$mode_environment" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 15
  bindsym $mod+Control+1 move window to workspace 15
  bindsym $mod+2 workspace 16; exec --no-startup-id echo 16 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 16
  bindsym $mod+Control+2 move window to workspace 16
  bindsym $mod+3 workspace 17; exec --no-startup-id echo 17 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 17
  bindsym $mod+Control+3 move window to workspace 17
  bindsym $mod+4 workspace 18; exec --no-startup-id echo 18 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 18
  bindsym $mod+Control+4 move window to workspace 18
  bindsym $mod+5 workspace 19; exec --no-startup-id echo 19 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 19
  bindsym $mod+Control+5 move window to workspace 19
  bindsym $mod+d workspace 15
  bindsym $mod+f workspace 14
  bindsym $mod+s workspace 12
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh


set $mode_project_4 Project_4
mode "$mode_project_4" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 25
  bindsym $mod+Control+1 move window to workspace 25
  bindsym $mod+2 workspace 26; exec --no-startup-id echo 26 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 26
  bindsym $mod+Control+2 move window to workspace 26
  bindsym $mod+3 workspace 27; exec --no-startup-id echo 27 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 27
  bindsym $mod+Control+3 move window to workspace 27
  bindsym $mod+4 workspace 28; exec --no-startup-id echo 28 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 28
  bindsym $mod+Control+4 move window to workspace 28
  bindsym $mod+5 workspace 29; exec --no-startup-id echo 29 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 29
  bindsym $mod+Control+5 move window to workspace 29
  bindsym $mod+d workspace 25
  bindsym $mod+f workspace 24
  bindsym $mod+s workspace 22
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh


set $mode_project_3 Project_3
mode "$mode_project_3" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 35
  bindsym $mod+Control+1 move window to workspace 35
  bindsym $mod+2 workspace 36; exec --no-startup-id echo 36 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 36
  bindsym $mod+Control+2 move window to workspace 36
  bindsym $mod+3 workspace 37; exec --no-startup-id echo 37 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 37
  bindsym $mod+Control+3 move window to workspace 37
  bindsym $mod+4 workspace 38; exec --no-startup-id echo 38 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 38
  bindsym $mod+Control+4 move window to workspace 38
  bindsym $mod+5 workspace 39; exec --no-startup-id echo 39 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 39
  bindsym $mod+Control+5 move window to workspace 39
  bindsym $mod+d workspace 35
  bindsym $mod+f workspace 34
  bindsym $mod+s workspace 32
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh


set $mode_project_2 Project_2
mode "$mode_project_2" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 45
  bindsym $mod+Control+1 move window to workspace 45
  bindsym $mod+2 workspace 46; exec --no-startup-id echo 46 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 46
  bindsym $mod+Control+2 move window to workspace 46
  bindsym $mod+3 workspace 47; exec --no-startup-id echo 47 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 47
  bindsym $mod+Control+3 move window to workspace 47
  bindsym $mod+4 workspace 48; exec --no-startup-id echo 48 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 48
  bindsym $mod+Control+4 move window to workspace 48
  bindsym $mod+5 workspace 49; exec --no-startup-id echo 49 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 49
  bindsym $mod+Control+5 move window to workspace 49
  bindsym $mod+d workspace 45
  bindsym $mod+f workspace 44
  bindsym $mod+s workspace 42
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh


set $mode_project_1 Project_1
mode "$mode_project_1" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 55
  bindsym $mod+Control+1 move window to workspace 55
  bindsym $mod+2 workspace 56; exec --no-startup-id echo 56 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 56
  bindsym $mod+Control+2 move window to workspace 56
  bindsym $mod+3 workspace 57; exec --no-startup-id echo 57 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 57
  bindsym $mod+Control+3 move window to workspace 57
  bindsym $mod+4 workspace 58; exec --no-startup-id echo 58 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 58
  bindsym $mod+Control+4 move window to workspace 58
  bindsym $mod+5 workspace 59; exec --no-startup-id echo 59 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 59
  bindsym $mod+Control+5 move window to workspace 59
  bindsym $mod+d workspace 55
  bindsym $mod+f workspace 54
  bindsym $mod+s workspace 52
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh


set $mode_project_0 Project_0
mode "$mode_project_0" {
  # use xev to determine correct keys if these don't work
  bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle
  
  # Sreen brightness controls
  # i3 doesn't seem to let you modify the audio buttons
  #"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
  #"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
  bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
  bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
  
  # Touchpad controls
  bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad
  
  # Media player controls
  bindsym XF86AudioPlay exec playerctl play
  bindsym XF86AudioPause exec playerctl pause
  bindsym XF86AudioNext exec playerctl next
  bindsym XF86AudioPrev exec playerctl previous

  bindsym $mod+1 workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace
  bindsym $mod+Shift+1 exec --no-startup-id swap_workspace 65
  bindsym $mod+Control+1 move window to workspace 65
  bindsym $mod+2 workspace 66; exec --no-startup-id echo 66 > ~/.i3_workspace
  bindsym $mod+Shift+2 exec --no-startup-id swap_workspace 66
  bindsym $mod+Control+2 move window to workspace 66
  bindsym $mod+3 workspace 67; exec --no-startup-id echo 67 > ~/.i3_workspace
  bindsym $mod+Shift+3 exec --no-startup-id swap_workspace 67
  bindsym $mod+Control+3 move window to workspace 67
  bindsym $mod+4 workspace 68; exec --no-startup-id echo 68 > ~/.i3_workspace
  bindsym $mod+Shift+4 exec --no-startup-id swap_workspace 68
  bindsym $mod+Control+4 move window to workspace 68
  bindsym $mod+5 workspace 69; exec --no-startup-id echo 69 > ~/.i3_workspace
  bindsym $mod+Shift+5 exec --no-startup-id swap_workspace 69
  bindsym $mod+Control+5 move window to workspace 69
  bindsym $mod+d workspace 65
  bindsym $mod+f workspace 64
  bindsym $mod+s workspace 62
  bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10

  bindsym $mod+minus mode "$mode_environment";workspace 15; exec --no-startup-id echo 15 > ~/.i3_workspace;exec --no-startup-id echo environment > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/environment_config.sh ~/.current_config.sh
  bindsym $mod+0 mode "$mode_project_4";workspace 25; exec --no-startup-id echo 25 > ~/.i3_workspace;exec --no-startup-id echo Project_4 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_4_config.sh ~/.current_config.sh
  bindsym $mod+9 mode "$mode_project_3";workspace 35; exec --no-startup-id echo 35 > ~/.i3_workspace;exec --no-startup-id echo Project_3 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_3_config.sh ~/.current_config.sh
  bindsym $mod+8 mode "$mode_project_2";workspace 45; exec --no-startup-id echo 45 > ~/.i3_workspace;exec --no-startup-id echo Project_2 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_2_config.sh ~/.current_config.sh
  bindsym $mod+7 mode "$mode_project_1";workspace 55; exec --no-startup-id echo 55 > ~/.i3_workspace;exec --no-startup-id echo Project_1 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_1_config.sh ~/.current_config.sh
  bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
}
bindsym $mod+6 mode "$mode_project_0";workspace 65; exec --no-startup-id echo 65 > ~/.i3_workspace;exec --no-startup-id echo Project_0 > ~/.i3_project;exec --no-startup-id ln -sf ~/.config/i3/Project_0_config.sh ~/.current_config.sh
bindsym $mod+Return exec konsole
  bindsym $mod+Shift+Return split h; exec konsole; split v
  bindsym $mod+Shift+q kill
  bindsym $mod+Shift+a split toggle; exec dmenu_run; split toggle
  bindsym $mod+a exec --no-startup-id i3-dmenu-desktop
  bindsym $mod+h focus left
  bindsym $mod+j focus down
  bindsym $mod+k focus up
  bindsym $mod+l focus right
  bindsym $mod+Left focus left
  bindsym $mod+Down focus down
  bindsym $mod+Up focus up
  bindsym $mod+Right focus right
  bindsym $mod+Shift+Left workspace prev
  bindsym $mod+Shift+Right workspace next
  bindsym $mod+b split t
  bindsym $mod+Shift+f fullscreen
  bindsym $mod+Shift+space floating toggle
  bindsym $mod+space focus mode_toggle
  bindsym $mod+backslash workspace 100: WorkConsole; exec --no-startup-id echo 100: WorkConsole > ~/.i3_workspace
  bindsym $mod+p workspace 14: Firefox; exec --no-startup-id echo 14: Firefox > ~/.i3_workspace
  bindsym $mod+o workspace 13: Slack; exec --no-startup-id echo 13: Slack > ~/.i3_workspace
  bindsym $mod+i workspace 12: Pidgin; exec --no-startup-id echo 12: Pidgin > ~/.i3_workspace
  bindsym $mod+Shift+u workspace 11: Keepass; exec --no-startup-id echo 11: Keepass > ~/.i3_workspace
  bindsym $mod+u exec --no-startup-id i3exec keepass-dmenu         --database $homedir/Dropbox/puppies.kdbx         --label password         --cache-password $((86400 - `date +%s` % 86400 + 7*3600 ))

  bindsym $mod+y workspace 101: Calendar
  bindsym $mod+Shift+c reload
  bindsym $mod+Shift+r restart
  bindsym $mod+r mode "resize"
  bindsym $mod+m mode "$mode_movement"
  bindsym $mod+End mode "$mode_system"
  bindsym $mod+Escape exec custom_locker
  bindsym $mod+Prior exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
  bindsym $mod+Next exec --no-startup-id amixer -D pulse sset Master 5%- unmute
  bindsym $mod+Shift+Prior exec --no-startup-id xbacklight -inc 10
  bindsym $mod+Shift+Next exec --no-startup-id xbacklight -dec 10# use xev to determine correct keys if these don't work
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -D pulse sset Master 5%+ unmute
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -D pulse sset Master 5%- unmute
bindsym XF86AudioMute exec --no-startup-id amixer -D pulse sset Master toggle

# Sreen brightness controls
# i3 doesn't seem to let you modify the audio buttons
#"XF86AudioRaiseVolume" => "exec --no-startup-id xbacklight -inc 10",
#"XF86AudioLoweraiseVolume" => "exec --no-startup-id xbacklight -dec 10",
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  workspace_buttons no
        status_command i3status
        bindsym button1 nop
        bindsym button2 nop
        bindsym button3 nop
        bindsym button4 nop
        bindsym button5 nop
        bindsym button6 nop
        bindsym button7 nop
        bindsym button8 nop
        bindsym button9 nop
}
exec --no-startup-id nm-applet

assign [class="Firefox"] 14: Firefox
assign [class="Slack"] 13: Slack
assign [class="Pidgin"] 12: Pidgin
assign [class="Keepassx"] 11: Keepass
assign [title="work_console"] 100: WorkConsole

# startup apps
exec --no-startup-id keepassx 
exec --no-startup-id firefox
exec --no-startup-id /usr/bin/slack
exec --no-startup-id pidgin

exec --no-startup-id konsole --profile work_console
exec --no-startup-id cron_notify_setup

#screensaver + lock on screen saver
exec_always --no-startup-id xautolock -time 15 -locker custom_lock

